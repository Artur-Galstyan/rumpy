format_version = 1
welcome_message = """
Welcome to rumpy: build a NumPy/SciPy-style library in Rust.
zeros, ones, and full now live in src/creation/. Your next exercise is arange.
Read lessons/004-arange.md, then edit exercises/01_creation/004_arange.rs. Press h for hints.
"""
final_message = """
You reached the end of the currently published exercises.
Commit and push your attempt to main. The separate daily check publishes the next
exercise after a new learner commit, even if your current tests are not all green.
No new learner commit means no new exercise. Run git pull --ff-only for updates.
"""

[[exercises]]
name = "001_zeros"
dir = "01_creation"
skip_check_unsolved = true
test = true
strict_clippy = false
hint = """
Read only as far as you need.

Hint 1 - Shape:
Count the coordinate combinations, not the axes. A rectangular array needs one
stored value for each position. Preserve every axis, including axes of length one.

Hint 2 - Scalar versus empty:
An empty shape denotes one scalar. A shape containing a zero-length axis has no
positions. Your count must distinguish these two cases.

Hint 3 - Rust tools:
Iterator::product can combine axis lengths. Vec supports repetition of a value.
shape.to_vec() copies the shape so the result does not borrow the caller's input.

Hint 4 - Container:
Array::from_vec(flat_data, owned_shape) returns Result<Array, ShapeError>.
You may use expect with a useful message: this exercise restricts inputs to small,
valid shapes. Shape validation is scaffold code, not today's task.

Completed: your implementation now lives in src/creation/zeros.rs.
The old exercise tests the public API. A separate reference is in
solutions/01_creation/001_zeros.rs. The original hints remain above for review.
"""

[[exercises]]
name = "002_ones"
dir = "01_creation"
skip_check_unsolved = true
test = true
strict_clippy = false
hint = """
Read only as far as you need.

Hint 1 - Reuse:
Your completed rumpy::zeros already preserves the shape, allocates independent
storage, and distinguishes scalars from zero-length axes. Reuse that work.

Hint 2 - What changes:
The number and arrangement of positions stay the same. Only the stored values
change. Do not add values to an array that has a zero-length axis.

Hint 3 - Mutable access:
Bind the returned Array as mutable. Array::as_mut_slice gives access to its data
without changing its shape. A loop or a mutable-slice method can update values.

Hint 4 - Rust tools:
Slices have a fill method. Return the Array, not its borrowed slice.
The reference solution is solutions/01_creation/002_ones.rs; compare after an attempt.

Completed: your implementation now lives in src/creation/ones.rs.
The old exercise tests rumpy::ones. The original hints remain above for review.
"""

[[exercises]]
name = "003_full"
dir = "01_creation"
skip_check_unsolved = true
test = true
strict_clippy = false
hint = """
Read only as far as you need.

Hint 1 - Parameter:
Read lessons/003-full.md. The starting code copies your ones implementation.
Which fixed value must become a caller-supplied value? Keep the shape unchanged.

Hint 2 - Edge cases:
An empty shape is a scalar. A zero-length axis has no stored values.
Your zeros helper already handles these cases within the supported shape scope.

Hint 3 - Copy, not arithmetic:
The fill value can be negative zero, infinity, or NaN. Copy the value directly.
Arithmetic that appears equivalent for ordinary numbers can change those values.

Hint 4 - Return value:
Use mutable-slice access to change the stored values. Return the owned Array.
Remove the temporary todo! only when the function satisfies its contract.
The reference is solutions/01_creation/003_full.rs; compare after your attempt.

Completed: your implementation now lives in src/creation/full.rs.
The old exercise tests rumpy::full. Add a historical // TODO comment to that
runner so Rustlings author checks can accept it. Keep the assertions intact.
"""

[[exercises]]
name = "004_arange"
dir = "01_creation"
test = true
strict_clippy = false
hint = """
Read only as far as you need.

Hint 1 - Interval:
Read lessons/004-arange.md. The start is included only when it is below stop.
The stop is excluded. An empty result still has one axis of length zero.

Hint 2 - Step:
Reject a zero or negative step before you consider the interval. The required
panic message contains: step must be positive.

Hint 3 - Arithmetic:
An endpoint fits in i32, but a final increment or interval span might not.
Consider wider integer arithmetic before any addition or subtraction.

Hint 4 - Container:
Collect the values into owned f64 storage. Derive the one-axis shape from the
number of stored values. Array::from_vec checks that these agree.
The reference is solutions/01_creation/004_arange.rs; compare after your attempt.
"""
